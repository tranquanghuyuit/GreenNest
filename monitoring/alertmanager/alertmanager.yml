global:
  resolve_timeout: 5m

route:
  receiver: local-dev-null
  group_by:
    - alertname
    - instance
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 1h

receivers:
  - name: local-dev-null
